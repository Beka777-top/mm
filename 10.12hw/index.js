const newTitle =document.querySelector(".title");
newTitle.textContent = "Hello my firends! welcome"

const newInto = document.querySelector(".into");
newInto.textContent ="JavaScript is a multi-paradigm, dynamic language with types and operators, standard built-in objects, and methods. Its syntax is based on the Java and C languages — many structures from those languages apply to JavaScript as well. JavaScript supports object-oriented programming with object prototypes and classes."

const newFact = document.querySelector(".fact");
newFact.textContent = "The real name is not “Javascript”, but ECMAScript. It was born in 1995 and was created in 10 days by the programmer Brendan Eich."
